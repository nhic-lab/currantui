---
"@nhic/currantui": minor
---

Add the dashboard layout system: DashboardGrid/DashboardWidget (controlled CSS-grid layout with drag, resize, keyboard move/resize, live-region announcements, and built-in undo/redo), WidgetToolbar and WidgetPalette authoring chrome, and the pure lib/grid-layout engine (moveItem, resizeItem, removeItem, compact, findSlot, clamp, collides).
