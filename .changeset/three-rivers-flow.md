---
"@nhic/currantui-charts": minor
---

Add the axis chart family: `LineChart` (linear/smooth curve, optional point markers), `AreaChart` (light overlapping fills or stacked cumulative mode), and `ComboChart` (mixed bar/line marks per group with an optional secondary value axis for series on a different scale).
